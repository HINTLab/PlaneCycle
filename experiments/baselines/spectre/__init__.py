"""Minimal subset of the official SPECTRE code (https://github.com/...),
included for the paper's 3D-foundation-model baseline: only the ViT backbone
import path `spectre.models.vision_transformer` and its dependency closure are
retained.
"""
