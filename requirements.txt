# PlaneCycle dependencies (pip / uv). Use Python 3.11.
# Install with:  uv pip install -r requirements.txt   (or plain: pip install -r requirements.txt)
#
# torch/torchvision: the PyPI wheels bundle CUDA. If you need a specific CUDA
# build, install them first from the matching index, e.g.:
#   uv pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128

# --- core (always needed) ---
torch==2.10.0
torchvision==0.25.0
numpy==2.2.6
pandas==2.3.3
pyyaml==6.0.3
tqdm==4.67.1
pillow==12.1.0
wandb==0.24.0
medmnist==3.0.2
monai==1.5.2            # segmentation experiments (Dice loss / metrics)

# comparison baselines (ACS / SPECTRE / CT-FM) are NOT included — install them
# per method only if you reproduce those baselines; see README "Environment setup".
